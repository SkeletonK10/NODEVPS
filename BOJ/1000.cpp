#include <bits/stdc++.h>
#define FastIO ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);

using namespace std;

int main() {
	FastIO;
	int T = 1;
	//cin >> T;
	while (T--) {
		int n, m;
		cin >> n >> m;  // Input two integers
		cout << n + m;  // Output the sum
	}
}